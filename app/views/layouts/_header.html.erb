<%= link_to "Home", root_url %> | <%= link_to "All Events", events_url %> | 

<% if logged_in? %>
	Logged in as: <%= link_to @user.username, @user %> | 
	<%= link_to "Create event", new_event_url %> | 
	<%= link_to "Log out", logout_url, method: :delete %> 
<% else %>
	<%= link_to "Log in", login_url %> | 
	<%= link_to "Sign up", new_user_url %>
<% end %>
<p>